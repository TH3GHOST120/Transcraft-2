package modmuss50.mods.transcraft.addons.waila;

public class TranscraftProvider {// implements IWailaDataProvider {
/*
 * 
 * 
 * 
 * @Override public ItemStack getWailaStack(IWailaDataAccessor accessor,
 * IWailaConfigHandler config) { return null; }
 * 
 * @Override public List<String> getWailaHead(ItemStack itemStack, List<String>
 * currenttip, IWailaDataAccessor accessor, IWailaConfigHandler config) { return
 * currenttip; }
 * 
 * @Override public List<String> getWailaBody(ItemStack itemStack, List<String>
 * currenttip, IWailaDataAccessor accessor, IWailaConfigHandler config) { // if
 * (accessor.getBlock() == TranscraftBlocks.ixpGrinder) { // TileIXP te =
 * (TileIXP) accessor.getTileEntity(); // currenttip.add("Amount Of IXP: " +
 * te.CurrentIXPValue); // } if (accessor.getBlock() ==
 * TranscraftBlocks.ObsidianLever) { currenttip.add("State: " +
 * (accessor.getMetadata() < 8 ? "Off" : "On")); } return currenttip; }
 * 
 * @Override public List<String> getWailaTail(ItemStack itemStack, List<String>
 * currenttip, IWailaDataAccessor accessor, IWailaConfigHandler config) { return
 * currenttip; }
 * 
 * public static void callbackRegister(IWailaRegistrar registrar) {
 * FMLLog.info("[Transcraft Addons] Callback Register For Waila");
 * 
 * // registrar.registerBodyProvider(new TranscraftProvider(), //
 * TranscraftBlocks.ixpGrinder.blockID); registrar.registerBodyProvider(new
 * TranscraftProvider(), TranscraftBlocks.ObsidianLever.blockID); }
 */
}
